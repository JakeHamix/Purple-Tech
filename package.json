{
  "private": true,
  "version": "0.0.0",
  "name": "purple-hire-o-mat",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "start": "concurrently --kill-others \"npm run server:run\"",
    "server:run": "nodemon",
    "server:build":"tsc -p ./server",
    "server:apidoc": "apidoc --input server/routes/ --output server/docs/ --file-filters .*\\.ts",
    "test": "jest"
  }
}